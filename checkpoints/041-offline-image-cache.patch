--- a/src/ui/components.js
+++ b/src/ui/components.js
@@ -3,2 +3,3 @@
 export const h = escapeHTML;
+export const asset = path => globalThis.ASTRA_IMAGES?.[path] || path;
 export const STEP_NAMES = {setup:'Opening hand',untap:'Untap',upkeep:'Upkeep',draw:'Draw',main1:'Main 1',beginCombat:'Combat',attackers:'Attack',damage:'Damage',endCombat:'End combat',main2:'Main 2',end:'End',cleanup:'Cleanup'};
@@ -20,3 +21,3 @@
   return `<article class="card ${object.tapped?'is-tapped':''} ${selected?'is-selected':''} ${legal?'is-legal':''} ${options.small?'small-card':''}" data-card="${h(id)}" data-zone="${h(object.zone)}" style="${style}" aria-label="${h(card.name)}${object.tapped?', tapped':''}">
-    <button class="card-face" data-action="card" data-id="${h(id)}" aria-label="${h(card.name)}${object.tapped?', tapped':''}" title="${h(card.name)} — ${h(object.zone==='battlefield'?'Click for a mana ability; use the name to inspect':'Inspect and choose an action')}"><img src="${h(object.face && card.backImage ? card.backImage : card.image)}" alt="${h(card.name)}" loading="lazy" draggable="false"><span class="card-fallback">${h(card.name)}</span>${object.tapped?'<span class="tap-badge">Tapped</span>':''}${c.types.includes('Creature')?`<span class="pt-badge">${h(c.power)}/${h(c.toughness)}</span>`:''}</button>
+    <button class="card-face" data-action="card" data-id="${h(id)}" aria-label="${h(card.name)}${object.tapped?', tapped':''}" title="${h(card.name)} — ${h(object.zone==='battlefield'?'Click for a mana ability; use the name to inspect':'Inspect and choose an action')}"><img src="${h(asset(object.face && card.backImage ? card.backImage : card.image))}" alt="${h(card.name)}" loading="lazy" draggable="false"><span class="card-fallback">${h(card.name)}</span>${object.tapped?'<span class="tap-badge">Tapped</span>':''}${c.types.includes('Creature')?`<span class="pt-badge">${h(c.power)}/${h(c.toughness)}</span>`:''}</button>
     ${button(h(card.name),'inspect',`data-id="${h(id)}" title="Inspect ${h(card.name)}"`,'card-name')}
--- a/src/ui/views.js
+++ b/src/ui/views.js
@@ -1,3 +1,3 @@
 import { COLORS, STEPS, suggestPayment, manaCostText } from '../core/index.js';
-import { h, logo, button, cardHTML, manaPoolHTML, manaText, symbol, empty, STEP_NAMES, ZONE_NAMES } from './components.js';
+import { h, asset, logo, button, cardHTML, manaPoolHTML, manaText, symbol, empty, STEP_NAMES, ZONE_NAMES } from './components.js';
 export function headerHTML({g,ui}) {
@@ -58,3 +58,3 @@
     const land=c.types.includes('Land'),permissions=g.castingPermissions(o,land),abilities=g.abilities(o),canCast=['hand','command','graveyard','exile','libraryActive','libraryReserve'].includes(o.zone)&&permissions.length;
-    content=`${button(`<img class="inspector-image" src="${h(d.image)}" alt="${h(d.name)}">`,'zoom',`data-card-id="${h(d.id)}"`,'quiet')}<h2 class="inspect-title">${h(d.name)}</h2><div class="row wrap">${manaText(d.manaCost)}<span class="pill">MV ${c.manaValue}</span>${o.token?'<span class="pill gold">Token copy</span>':''}</div><div class="detail-line" style="margin-top:9px">${h([...c.supertypes,...c.types].join(' '))}${c.subtypes.length?` — ${h(c.subtypes.join(' '))}`:''}</div><div class="row wrap" style="margin-top:8px">${c.types.includes('Creature')?`<span class="pill gold">${c.power} / ${c.toughness}</span>`:''}${c.keywords.filter(k=>!['Landfall','Affinity'].includes(k)).map(k=>`<span class="pill">${h(k)}</span>`).join('')}</div><div class="oracle">${h(d.oracleText)}</div><div class="detail-line">${h(ZONE_NAMES[o.zone])} · Object ${h(o.id)}:${o.oid}${o.zone==='battlefield'?` · ${o.tapped?'Tapped':'Untapped'}${g.isSick(o)?' · Summoning sickness':''}`:''}</div>${o.attachedTo?`<div class="detail-line">Attached to ${h(g.object(o.attachedTo)?g.definition(g.object(o.attachedTo)).name:'a departed object')}</div>`:''}${Object.keys(o.counters).length?`<div class="detail-line">Counters: ${h(Object.entries(o.counters).map(([k,n])=>`${n} ${k}`).join(', '))}</div>`:''}<hr class="divider">${m.status!=='full'?`<div class="callout warning">${h(m.notes||'This card has not passed the pack’s automation acceptance.')}</div>`:''}${canCast?button(land?'Play this land':'Cast this spell','cast',`data-id="${h(o.id)}"`,'ability primary'):''}${m.plot&&o.zone==='hand'?button(`Plot ${h(m.plot)}`,'plot',`data-id="${h(o.id)}"`,'ability'):''}${abilities.map(a=>button(`${h(a.label||a.id)}<small>${a.tap?'Tap · ':''}${typeof a.cost==='string'?h(a.cost)+' · ':''}${a.mana?'Mana ability · no stack':a.loyalty!=null?'Loyalty ability':'Activated ability'}</small>`,'ability',`data-id="${h(o.id)}" data-ability="${h(a.id)}"`,'ability')).join('')}${!canCast&&!abilities.length?'<p class="muted">No activated action available from this zone. Static and triggered abilities are automatic.</p>':''}${optionalHTML(g,o)}<details><summary class="muted">Canonical rulings (${d.rulings?.length||0})</summary>${(d.rulings||[]).map(r=>`<p class="muted" style="margin:10px 0">${h(r.comment)}</p>`).join('')}</details>`;
+    content=`${button(`<img class="inspector-image" src="${h(asset(d.image))}" alt="${h(d.name)}">`,'zoom',`data-card-id="${h(d.id)}"`,'quiet')}<h2 class="inspect-title">${h(d.name)}</h2><div class="row wrap">${manaText(d.manaCost)}<span class="pill">MV ${c.manaValue}</span>${o.token?'<span class="pill gold">Token copy</span>':''}</div><div class="detail-line" style="margin-top:9px">${h([...c.supertypes,...c.types].join(' '))}${c.subtypes.length?` — ${h(c.subtypes.join(' '))}`:''}</div><div class="row wrap" style="margin-top:8px">${c.types.includes('Creature')?`<span class="pill gold">${c.power} / ${c.toughness}</span>`:''}${c.keywords.filter(k=>!['Landfall','Affinity'].includes(k)).map(k=>`<span class="pill">${h(k)}</span>`).join('')}</div><div class="oracle">${h(d.oracleText)}</div><div class="detail-line">${h(ZONE_NAMES[o.zone])} · Object ${h(o.id)}:${o.oid}${o.zone==='battlefield'?` · ${o.tapped?'Tapped':'Untapped'}${g.isSick(o)?' · Summoning sickness':''}`:''}</div>${o.attachedTo?`<div class="detail-line">Attached to ${h(g.object(o.attachedTo)?g.definition(g.object(o.attachedTo)).name:'a departed object')}</div>`:''}${Object.keys(o.counters).length?`<div class="detail-line">Counters: ${h(Object.entries(o.counters).map(([k,n])=>`${n} ${k}`).join(', '))}</div>`:''}<hr class="divider">${m.status!=='full'?`<div class="callout warning">${h(m.notes||'This card has not passed the pack’s automation acceptance.')}</div>`:''}${canCast?button(land?'Play this land':'Cast this spell','cast',`data-id="${h(o.id)}"`,'ability primary'):''}${m.plot&&o.zone==='hand'?button(`Plot ${h(m.plot)}`,'plot',`data-id="${h(o.id)}"`,'ability'):''}${abilities.map(a=>button(`${h(a.label||a.id)}<small>${a.tap?'Tap · ':''}${typeof a.cost==='string'?h(a.cost)+' · ':''}${a.mana?'Mana ability · no stack':a.loyalty!=null?'Loyalty ability':'Activated ability'}</small>`,'ability',`data-id="${h(o.id)}" data-ability="${h(a.id)}"`,'ability')).join('')}${!canCast&&!abilities.length?'<p class="muted">No activated action available from this zone. Static and triggered abilities are automatic.</p>':''}${optionalHTML(g,o)}<details><summary class="muted">Canonical rulings (${d.rulings?.length||0})</summary>${(d.rulings||[]).map(r=>`<p class="muted" style="margin:10px 0">${h(r.comment)}</p>`).join('')}</details>`;
   }
@@ -66,3 +66,3 @@
   if(!keys.length)return '';
-  return `<details style="margin:14px 0"><summary class="muted">Optional-effect preferences</summary>${keys.map(k=>{const key=`${o.copy?.rulesId||o.cardId}/${k.id}`;return `<label class="detail-line" style="margin-top:8px">${h(k.label)}<select data-policy="${h(key)}" style="display:block;width:100%;margin:4px 0">${(k.provisioner?['ASK','Treasure','Food']:['ASK','YES','NO']).map(v=>`<option ${(g.state.optionalPreferences[key]||'ASK')===v?'selected':''}>${v}</option>`).join('')}</select></label>`;}).join('')}</details>`;
+  return `<details style="margin:14px 0"><summary class="muted">Optional-effect preferences</summary>${keys.map(k=>{const key=`${o.copy?.rulesId||o.cardId}/${k.id}`;return `<label class="detail-line" style="margin-top:8px">${h(k.label)}<select data-policy="${h(key)}" style="display:block;width:100%;margin:4px 0">${(k.provisioner?['ASK','Treasure','Food']:['ASK','YES','NO']).map(v=>`<option ${(g.state.optionalPreferences[key]||(k.provisioner?'Treasure':'ASK'))===v?'selected':''}>${v}</option>`).join('')}</select></label>`;}).join('')}</details>`;
 }
--- a/src/ui/dialogs.js
+++ b/src/ui/dialogs.js
@@ -1,2 +1,2 @@
-import { h, button, logo, manaText, empty } from './components.js';
+import { h, asset, button, logo, manaText, empty } from './components.js';
 import { LABS } from './labs.js';
@@ -15,6 +15,6 @@
     const cards=registry.list().filter(c=>(ui.showDerived||c.candidate)&&(!type||c.types.includes(type))&&`${c.name} ${c.oracleText} ${c.typeLine}`.toLowerCase().includes(query)).sort((a,b)=>a.name.localeCompare(b.name));
-    body=`<div class="row wrap"><input class="grow" id="card-search" aria-label="Search card registry" placeholder="Search name, type or rules text…" value="${h(ui.cardFilter)}"><select id="card-type" aria-label="Filter card type"><option value="">All types</option>${['Artifact','Creature','Enchantment','Instant','Sorcery','Land','Planeswalker'].map(t=>`<option ${type===t?'selected':''}>${t}</option>`).join('')}</select></div><div class="spread" style="margin-top:14px"><span class="muted">${cards.length} matching definitions</span><label class="muted"><input type="checkbox" id="show-derived" ${ui.showDerived?'checked':''}> Include tokens and derived faces</label></div><div class="library-grid">${cards.map(c=>`<article class="registry-card"><button data-action="zoom" data-card-id="${h(c.id)}" aria-label="Read ${h(c.name)}"><img src="${h(c.image)}" alt="${h(c.name)}" loading="lazy"><div class="registry-caption">${h(c.name)}<small>${h(c.typeLine)} · MV ${c.manaValue}</small><small>${registry.module(c.id).status==='full'?'Registered rules':'Derived / scoped definition'}</small></div></button></article>`).join('')||empty('No matching cards','Try a broader search.')}</div>`;
+    body=`<div class="row wrap"><input class="grow" id="card-search" aria-label="Search card registry" placeholder="Search name, type or rules text…" value="${h(ui.cardFilter)}"><select id="card-type" aria-label="Filter card type"><option value="">All types</option>${['Artifact','Creature','Enchantment','Instant','Sorcery','Land','Planeswalker'].map(t=>`<option ${type===t?'selected':''}>${t}</option>`).join('')}</select></div><div class="spread" style="margin-top:14px"><span class="muted">${cards.length} matching definitions</span><label class="muted"><input type="checkbox" id="show-derived" ${ui.showDerived?'checked':''}> Include tokens and derived faces</label></div><div class="library-grid">${cards.map(c=>`<article class="registry-card"><button data-action="zoom" data-card-id="${h(c.id)}" aria-label="Read ${h(c.name)}"><img src="${h(asset(c.image))}" alt="${h(c.name)}" loading="lazy"><div class="registry-caption">${h(c.name)}<small>${h(c.typeLine)} · MV ${c.manaValue}</small><small>${registry.module(c.id).status==='full'?'Registered rules':'Derived / scoped definition'}</small></div></button></article>`).join('')||empty('No matching cards','Try a broader search.')}</div>`;
   } else if(ui.modal==='card') {
     const c=registry.get(ui.zoomCard);title=c.name;subtitle=c.typeLine;
-    body=`<div class="guide-grid"><div><img src="${h(ui.backFace&&c.backImage?c.backImage:c.image)}" alt="${h(c.name)}" style="width:100%;border-radius:13px">${c.backImage?button('Show other face','flip','','ability'):''}</div><div><div class="row wrap">${manaText(c.manaCost)}<span class="pill">Mana value ${c.manaValue}</span></div><div class="oracle">${h(c.oracleText)}</div><p class="muted">Oracle ID ${h(c.id)}<br>Printing ${h(c.set||'')} ${h(c.collectorNumber||'')}</p><h3>Canonical rulings</h3>${(c.rulings||[]).map(r=>`<p class="muted">${h(r.comment)}</p>`).join('')||'<p class="muted">No additional rulings in this local snapshot.</p>'}</div></div>`;
+    body=`<div class="guide-grid"><div><img src="${h(asset(ui.backFace&&c.backImage?c.backImage:c.image))}" alt="${h(c.name)}" style="width:100%;border-radius:13px">${c.backImage?button('Show other face','flip','','ability'):''}</div><div><div class="row wrap">${manaText(c.manaCost)}<span class="pill">Mana value ${c.manaValue}</span></div><div class="oracle">${h(c.oracleText)}</div><p class="muted">Oracle ID ${h(c.id)}<br>Printing ${h(c.set||'')} ${h(c.collectorNumber||'')}</p><h3>Canonical rulings</h3>${(c.rulings||[]).map(r=>`<p class="muted">${h(r.comment)}</p>`).join('')||'<p class="muted">No additional rulings in this local snapshot.</p>'}</div></div>`;
     footer=button('Back','zoom-back');
--- a/tools/build.mjs
+++ b/tools/build.mjs
@@ -21,9 +21,12 @@
 for(const card of cards){if(!fs.existsSync(path.join(root,card.image)))throw new Error(`Missing image: ${card.name}`);if(card.backImage&&!fs.existsSync(path.join(root,card.backImage)))throw new Error(`Missing back image: ${card.name}`);}
+const standalone=process.argv.includes('--standalone');
+const embedded=standalone?Object.fromEntries([...new Set(cards.flatMap(c=>[c.image,c.backImage].filter(Boolean)))].map(p=>[p,'data:image/jpeg;base64,'+fs.readFileSync(path.join(root,p)).toString('base64')])):null;
 const payload=JSON.stringify({cards,pool,deckText:read('data/candidate-pool.txt')}).replace(/</g,'\\u003c');
 const runtime=`(()=>{'use strict';const factories={${[...modules].map(([id,s])=>`${JSON.stringify(id)}:(module,exports,require)=>{\n${s}\n}`).join(',\n')}};const cache={};function require(id){if(cache[id])return cache[id].exports;if(!factories[id])throw new Error('Missing module '+id);const module=cache[id]={exports:{}};factories[id](module,module.exports,require);return module.exports;}require('src/ui/app.js');})();`;
-const html=`<!doctype html>\n<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="dark"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src 'self' data: blob: file:; connect-src 'none'; base-uri 'none'; form-action 'none'"><meta name="description" content="An offline, rules-aware Magic goldfish laboratory for the supplied 160-card candidate pool."><title>Astra · The Goldfish Lab</title><link rel="icon" href="data:,"><style>${read('src/ui/styles.css')}</style></head><body><div id="app"><div class="starting">Opening your laboratory…</div></div><div id="overlay"></div><noscript>JavaScript is required. No network connection is needed.</noscript><script>window.ASTRA_DATA=${payload};</script><script>${runtime.replace(/<\/script/gi,'<\\/script')}</script></body></html>\n`;
-fs.writeFileSync(path.join(root,'index.html'),html);
+const html=`<!doctype html>\n<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="dark"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src 'self' data: blob: file:; connect-src 'none'; base-uri 'none'; form-action 'none'"><meta name="description" content="An offline, rules-aware Magic goldfish laboratory for the supplied 160-card candidate pool."><title>Astra · The Goldfish Lab</title><link rel="icon" href="data:,"><style>${read('src/ui/styles.css')}</style></head><body><div id="app"><div class="starting">Opening your laboratory…</div></div><div id="overlay"></div><noscript>JavaScript is required. No network connection is needed.</noscript><script>${embedded?`window.ASTRA_IMAGES=${JSON.stringify(embedded)};`:""}window.ASTRA_DATA=${payload};</script><script>${runtime.replace(/<\/script/gi,'<\\/script')}</script></body></html>\n`;
+const output=standalone?'Astra-standalone.html':'index.html';
+fs.writeFileSync(path.join(root,output),html);
 fs.mkdirSync(path.join(root,'test-results'),{recursive:true});
-const report={build:'standalone-offline',modules:modules.size,candidateCards:cards.filter(c=>c.candidate).length,definitions:cards.length,htmlBytes:Buffer.byteLength(html),sha256:createHash('sha256').update(html).digest('hex'),runtimeDependencies:[],networkRequired:false};
-fs.writeFileSync(path.join(root,'test-results/build.json'),JSON.stringify(report,null,2)+'\n');
+const report={build:standalone?'single-file-offline':'offline-with-local-assets',output,modules:modules.size,candidateCards:cards.filter(c=>c.candidate).length,definitions:cards.length,htmlBytes:Buffer.byteLength(html),sha256:createHash('sha256').update(html).digest('hex'),runtimeDependencies:[],networkRequired:false};
+fs.writeFileSync(path.join(root,`test-results/build${standalone?'-standalone':''}.json`),JSON.stringify(report,null,2)+'\n');
 console.log(JSON.stringify(report,null,2));
