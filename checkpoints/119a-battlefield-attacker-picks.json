{
  "format": "astra-text-edits-v1",
  "files": {
    "src/tabletop/app.js": {
      "baseSha256": "32f3ec5d1eba0197e4de62c5465d4563f6f295422e111d20024a3a6218922a02",
      "sha256": "f63368ec930cae887df982b72845fdaa6715c5f15b8b78bde990baab8fcc8552",
      "edits": [
        {"old":"Grids,playerPrograms:doc?.playerPrograms});\n}\nboot().catch(error=>{root.innerHTM","new":"Grids,playerPrograms:doc?.playerPrograms,attackDraft:doc?.attackDraft});\n}\nboot().catch(error=>{root.innerHTM"},
        {"old":"(){return store;},exported,version:'1.4.9'};\nasync function boot(){await store.op","new":"(){return store;},exported,version:'1.4.10'};\nasync function boot(){await store.op"},
        {"old":"s[p][field]});}\n if(el.dataset.attacker){el.checked?ui.selected.add(el.dataset.attacker):ui.selected.delete(el.dataset.attacker);}if(el.dataset.destination)ui.attacks[el.dataset.destination]=Number(el.value);\n if(el.id==='session-file')importFile","new":"s[p][field]});}\n if(el.dataset.attacker)editAttackPick(el.dataset.attacker,{selected:el.checked});if(el.dataset.destination)editAttackPick(el.dataset.destination,{player:Number(el.value)});\n if(el.id==='session-file')importFile"},
        {"old":"ackers});if(result.ok){ui.modal=null;ui.selected.clear();render();autoResolve();}return;}\n if(a","new":"ackers});if(result.ok){ui.modal=null;ui.attackDraft=cleanAttackDraft(g,null);render();autoResolve();}return;}\n if(a"},
        {"old":"return;}\n if(action==='attack-confirm'){const attackers=g.controlled().filter(o=>ui.selected.has(o.id)&&g.characteristics(o).types.includes('Creature')).map(o=>({id:o.id,player:ui.attacks[o.id]||1}));const result=run({type:'DECLARE_ATTACK","new":"return;}\n if(action==='attack-confirm'){if(!attackSelectionReady(g)){toast('Finish the stack or pending choice before declaring attackers.',true);return;}const attackers=selectedAttackers(g,ui.attackDraft);const result=run({type:'DECLARE_ATTACK"},
        {"old":"yerPrograms:prior.session.playerPrograms});toast('Previous autosave recovered.')","new":"yerPrograms:prior.session.playerPrograms,attackDraft:prior.session.attackDraft});toast('Previous autosave recovered.')"},
        {"old":"mulligan')return run({type:'MULLIGAN'});\n if(action==='phase')return run({type:'","new":"mulligan')return run({type:'MULLIGAN'});\n // A phase shortcut must not silently throw away attackers just selected on\n // the table. Review/declare them first; an empty draft retains normal Step.\n if(['phase','next','next-player','next-turn','opponent'].includes(action)&&attackSelectionReady(g)&&selectedAttackers(g,ui.attackDraft).length&&!(action==='phase'&&el.dataset.step==='attackers')){openDialog('combat');return;}\n if(action==='attack-clear'){ui.attackDraft=cleanAttackDraft(g,null);refreshAttackSelection();if(ui.modal==='combat')renderModal();return;}\n if(action==='phase')return run({type:'"},
        {"old":"eGrids,playerPrograms:doc.playerPrograms});toast('Session restored, including un","new":"eGrids,playerPrograms:doc.playerPrograms,attackDraft:doc.attackDraft});toast('Session restored, including un"},
        {"old":".state.pending&&selectChoice(id))return;\n if(ui.inspected===id){clearInspectorSt","new":".state.pending&&selectChoice(id))return;\n const creature=g.object(id);\n if(attackSelectionReady(g)&&creature?.zone==='battlefield'&&g.characteristics(creature).types.includes('Creature')){editAttackPick(id);return;}\n if(ui.inspected===id){clearInspectorSt"},
        {"old":"hoice]);\n   }catch{}\n }\n return true;\n}\nfunction clickCard(id,point,shift=false)","new":"hoice]);\n   }catch{}\n }\n return true;\n}\n// Update only the attack controls and borders while picking. Do not rebuild\n// the battlefield, steal keyboard focus or put UI-only picks in undo history.\nfunction refreshAttackSelection(){\n ui.attackDraft=cleanAttackDraft(g,ui.attackDraft);\n for(const el of document.querySelectorAll('[data-surface=\"battlefield\"] [data-card]')){\n  const picked=!!ui.attackDraft.cards[el.dataset.card]?.selected;\n  el.classList.toggle('attack-selected',picked);\n  const face=el.querySelector('.face');if(face?.hasAttribute('aria-pressed'))face.setAttribute('aria-pressed',String(picked));\n }\n const controls=document.querySelector('.attack-controls');if(controls)controls.innerHTML=attackerControls(ctx());\n const confirm=overlay.querySelector('[data-action=\"attack-confirm\"]');if(confirm)confirm.textContent=`Declare selected attackers (${selectedAttackers(g,ui.attackDraft).length})`;\n targetLinks.schedule();scheduleSave();\n}\nfunction editAttackPick(id,options){\n if(programs.running){toast('Stop the running sequence before selecting attackers.',true);return;}\n const result=options?setAttackPick(g,ui.attackDraft,id,options):toggleAttackPick(g,ui.attackDraft,id);\n ui.attackDraft=result.draft;\n if(result.error){toast(result.error,true);return;}\n programs.userAction();\n if(ui.inspected){clearInspectorState();render();scheduleSave();}else refreshAttackSelection();\n}\nfunction clickCard(id,point,shift=false)"},
        {"old":"ndering=true;try{\n syncInspectorState();\n const focus=focusSnapshot(),scroll=new","new":"ndering=true;try{\n syncInspectorState();ui.attackDraft=cleanAttackDraft(g,ui.attackDraft);\n const focus=focusSnapshot(),scroll=new"},
        {"old":"l,stackLabel:null,modal:null,noteText:'',layouts:{},memo:cleanMemo(memo),grids:c","new":"l,stackLabel:null,modal:null,noteText:'',attackDraft:cleanAttackDraft(g,attackDraft),layouts:{},memo:cleanMemo(memo),grids:c"},
        {"old":"rue,memo={},grids={},playerPrograms=null}={}){unsubscribe?.();clearTimeout(autoT","new":"rue,memo={},grids={},playerPrograms=null,attackDraft=null}={}){unsubscribe?.();clearTimeout(autoT"},
        {"old":"rids),playerPrograms:programs.snapshot()};}\nlet saveRevision=0,savedRevision=-1,","new":"rids),playerPrograms:programs.snapshot(),attackDraft:cleanAttackDraft(g,ui.attackDraft)};}\nlet saveRevision=0,savedRevision=-1,"},
        {"old":"paymentEdited:false,\n noteText:'',attacks:{},gestureActive:false,activeZone:'battlef","new":"paymentEdited:false,\n noteText:'',attackDraft:null,gestureActive:false,activeZone:'battlef"},
        {"old":"js';\nimport { toolbar, programControls, tabletop, stackPopup, inspectorPopup, de","new":"js';\nimport { toolbar, programControls, attackerControls, tabletop, stackPopup, inspectorPopup, de"},
        {"old":"import { workspaceSnapshot, workspaceTra","new":"import { attackCombatKey, attackSelectionReady, cleanAttackDraft, selectedAttackers, setAttackPick, toggleAttackPick } from './attack-selection.js';\nimport { workspaceSnapshot, workspaceTra"}
      ]
    },
    "src/tabletop/views.js": {
      "baseSha256":"24d2166b5922c310873e2bb1cfe3553af3bf274280b2c8421a99ca69680ba5fc",
      "sha256":"7857946ffcfe56891e9ce52b9dc2d630f7c65f6971f8be9f5467b328eaa4cfe8",
      "edits":[
        {"old":"ture(g,c.id,{preview:zone==='workspace',selected:ui.selected.has(c.id)||ui.choic","new":"ture(g,c.id,{preview:zone==='workspace',attackMode:zone==='battlefield'&&!!attackCombatKey(g)&&g.characteristics(c.id).types.includes('Creature'),attackSelected:zone==='battlefield'&&!!ui.attackDraft?.cards[c.id]?.selected,selected:ui.selected.has(c.id)||ui.choic"},
        {"old":"ne)}\"><button type=\"button\" class=\"face\" data-action=\"card\" data-id=\"${h(id)}\" a","new":"ne)}\"><button type=\"button\" class=\"face\" ${extra.attackMode?`aria-pressed=\"${!!extra.attackSelected}\"`:''} data-action=\"card\" data-id=\"${h(id)}\" a"},
        {"old":"${extra.selected?'selected':''} ${extra.legal?'legal':''} ${extra.classes||''}\" ","new":"${extra.selected?'selected':''} ${extra.attackSelected?'attack-selected':''} ${extra.legal?'legal':''} ${extra.classes||''}\" "},
        {"old":"'next-turn',!s.started?'disabled':'')}${s.activePlayer===0&&s.step==='attackers'?button('Attack','dialog','data-dialog=\"combat\"'):''}${s.step==='damage'&&s.activePlayer=","new":"'next-turn',!s.started?'disabled':'')}${attackCombatKey(g)?`<span class=\"attack-controls\" role=\"group\" aria-label=\"Select attackers\">${attackerControls({g,ui})}</span>`:''}${s.step==='damage'&&s.activePlayer="},
        {"old":"bel=\"Resume automation\"`,'active'):''}`;}\nfunction fullToolbar({g,ui,prefs,progr","new":"bel=\"Resume automation\"`,'active'):''}`;}\nexport function attackerControls({g,ui}){\n if(!attackCombatKey(g))return '';\n const count=selectedAttackers(g,ui.attackDraft).length,ready=attackSelectionReady(g);\n return `<span class=\"attack-hint\">${ready?'Click creatures to select attackers':'Resolve the stack / choice first'}</span>${button(`Declare attackers (${count})`,'attack-confirm',`${ready?'':'disabled'} title=\"Declare the highlighted creatures together; Attack options changes their opponents\"`,'primary')}${button('Attack options','dialog','data-dialog=\"combat\"')}${count?button('Clear','attack-clear','title=\"Deselect all attackers\"'):''}`;\n}\nfunction fullToolbar({g,ui,prefs,progr"},
        {"old":"import { canFollow, followsAttachment, p","new":"import { attackCombatKey, attackSelectionReady, selectedAttackers } from './attack-selection.js';\nimport { canFollow, followsAttachment, p"}
      ]
    },
    "src/tabletop/dialogs.js":{
      "baseSha256":"57d505babc7a82918949b261511af72eef8a0f65df49feafa1b5bf3d64ee5911",
      "sha256":"b65dacd9b208421aa20ce143ff27dc2a56ba1030abab2814a366b644fd0078f0",
      "edits":[
        {"old":"ver taps anything automatically.</p><h3>Move and stack</h3><p>The grabbed point ","new":"ver taps anything automatically.</p><h3>Select attackers</h3><p>In your Attack step, click a creature to select it with an orange border; click again to deselect. Right-click still inspects. Press Declare attackers to commit all picks together, or use Attack options to change their opponents. Selection never taps creatures or fires triggers. Pending target and payment choices take precedence over attacker clicks.</p><h3>Move and stack</h3><p>The grabbed point "},
        {"old":"tle='Astra';sub='The Goldfish Lab · 1.4.9';narrow=true;body=menuBody(ctx);}\n  else if(ui.modal==='sequences'){titl","new":"tle='Astra';sub='The Goldfish Lab · 1.4.10';narrow=true;body=menuBody(ctx);}\n  else if(ui.modal==='combat'){\n    title='Declare attackers';sub='Click creatures on the battlefield to select or deselect. Right-click to inspect.';\n    const ready=attackSelectionReady(g),draft=ui.attackDraft,creatures=g.controlled().filter(o=>g.characteristics(o).types.includes('Creature'));\n    body=creatures.map(o=>{const problem=attackerProblem(g,o),pick=draft?.cards[o.id],name=g.definition(o).name;\n      return `<div class=\"setting-row\"><input id=\"attack-pick-${h(o.id)}\" type=\"checkbox\" data-attacker=\"${h(o.id)}\" ${pick?.selected?'checked':''} ${problem||!ready?'disabled':''}><label class=\"grow\" for=\"attack-pick-${h(o.id)}\">${h(name)} <small>${g.characteristics(o).power}/${g.characteristics(o).toughness}${problem?' · '+h(problem):''}</small></label><select data-destination=\"${h(o.id)}\" aria-label=\"Attack destination for ${h(name)}\" ${problem||!ready?'disabled':''}>${s.players.filter(p=>p.id>0&&!p.lost).map((p,i)=>`<option value=\"${p.id}\" ${(pick?.player===p.id||(!pick&&i===0))?'selected':''}>${h(p.name)}</option>`).join('')}</select></div>`;\n    }).join('')||'<p>No creatures on the battlefield.</p>';\n    body+=`<p class=\"muted\">${ready?'No creatures tap and no attack triggers fire until you declare. All selected attackers are declared together.':'Attacker selection is unavailable while a choice or stack is pending, or after attackers have been declared.'}</p>`;\n    footer=button('Return to battlefield','close-dialog')+button(`Declare selected attackers (${selectedAttackers(g,draft).length})`,'attack-confirm',ready?'':'disabled','primary');\n  }\n  else if(ui.modal==='sequences'){titl"},
        {"old":"import { sequenceBody,automationBody } f","new":"import { attackSelectionReady, attackerProblem, selectedAttackers } from './attack-selection.js';\nimport { sequenceBody,automationBody } f"}
      ]
    },
    "src/tabletop/styles.css":{
      "baseSha256":"fad3115ceab9eb09080442fec375520bf4e4596aa74cb9ccac394b6280fee376",
      "sha256":"8c68ee756d154a8640a5d1393849e45fa1b13eb713563ae1abb8a8a2f6cbb92c",
      "edits":[{"old":"kspace-window .popup-resize{z-index:10}\n","new":"kspace-window .popup-resize{z-index:10}\n\n/* Distinct from target/drag selection; the border is part of the scaled card. */\n.playing-card.attack-selected .face{outline:3px solid #ff996b;outline-offset:2px;box-shadow:0 0 0 5px #6b2519,0 0 15px #e85d3577}\n.attack-controls{display:inline-flex;gap:5px;align-items:center;flex-wrap:wrap}\n.attack-hint{font-size:11px;color:var(--muted);padding:0 4px}\n.toolbar .turn-tools{flex-wrap:wrap;max-width:100%}\n.toolbar .attack-controls button.primary{color:#151b1e;background:var(--gold);border-color:var(--gold)}\n.toolbar .attack-controls button.primary:hover:not(:disabled){background:#e9c994}\n"}]
    }
  }
}
